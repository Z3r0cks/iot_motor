#include <Quiz.h>

Quiz::Quiz(uint8_t pinA,
           uint8_t pinB,
           uint8_t pinC,
           uint8_t pinD,
           uint8_t pinData){

};

void Quiz::wakeUp() {}

void Quiz::chooseCategory() {
    if (this->isGameMaster) {
    } else {
    }
}
